---
wiki-ingest:
  status: pending
---

# 디자인 시스템 층 소유 경계 — held-out 5라운드 검증 원장

**일자** 2026-07-27 · **Tier** 자동 T3(메타 워크플로우) · **maker** dakman-setup 진행자 · **held-out** codex(gpt-5.5 high) · grok(4.5 high) · claude-debater(Opus 5)
**발단** dakman-oraebom 발견 → dakman-design-system 위임 · **정본** `.claude/workflows/workflow.md` 원칙 8 / §0 / §10.2 / §11 / §12

---

## 1. 핵심 결론

**템플릿 정본이 "4레이어·토큰"만 말하고 누가 어느 층을 소유하는지 말하지 않아서, 이 템플릿으로 깔린 제품 프로젝트가 "자기 repo에서 DS·토큰을 정의하라"로 읽었다.** 중앙 DS가 토큰 정본을 갖는 구조와 정면 충돌한다. 무증상이었던 이유는 오라이봄이 Phase 0이라 아직 그 파일을 안 만들었기 때문이고, Phase 1에서 실제로 쓰는 순간 토큰 정본이 둘로 갈라진다 — **그때 고치면 이미 값이 갈라진 뒤다.**

교정: 층 소유를 `{중앙DS}` 변수(§0)로 **3분기 규범화**하고, 산출물 문서를 *DS 정의*에서 **DS 소비 계약**으로 재정의했다.

---

## 2. 무엇이 바뀌었나

| 위치 | before | after |
|------|--------|-------|
| **원칙 8** | "Foundation → Component → Pattern → Screen 4 레이어, 토큰 단일 소스." (소유 주체 없음) | 층 소유를 `{중앙DS}`(§0)가 정함 — **repo·판**=F·C 중앙 / P·S 제품, 참조 **제품→중앙 단방향** · **`없음`**=제품 4층 · **`본체`**=이 repo가 F·C 정본(P·S는 소비 제품). 접근성 바닥은 P·S에서도 강제, **소유자가 발명 금지·WCAG·ARIA 인용** |
| **§0** | — | **`{중앙DS}` 변수 신설** — 중앙 DS 관계값(repo·판 / `없음` / `본체`). **프로젝트 시작 시 확정**(읽는 시점 자기선언 차단) |
| **§11 산출물 표** | `디자인 시스템 4 레이어, 토큰` | **DS 소비 계약** — 소비 판 + 제품 소유 P·S. `없음`=4층 자체 정의 / `본체`=정본 위치 명시 / **DS 미사용=`n/a` 비움** |
| **§10.2** | — | **DS 층 경계 준수** — 토큰 재정의 건수(목표 0, `없음`·`본체` 제외) ↔ **counter-metric**: 경계로 막힌 제품 작업 건수 |
| **§11 출처** | — | emotion-design-system `lessons.md` R-6 · dakman-design-system `lessons.md` 함정 클래스 11-가 (2행 등재) |
| **`docs/project-init.md`** | "디자인 시스템 4 레이어"만 | 층 소유 3분기 + §0 목록에 `{중앙DS}`·`{E2E실행}`·`{실행환경}` |

**dakman-setup 도메인 판단(요청 스펙 밖)**: 이 템플릿은 *공용*이라 중앙 DS 없는 프로젝트에도 깔린다 → 경계를 무조건문으로 쓰면 그 프로젝트가 자기 토큰을 못 만드는 모순. **조건 분기**로 썼다.

---

## 3. 검증 원장 (held-out 5라운드)

| R | codex | grok | claude | 그 라운드가 잡은 것 |
|:-:|:-----:|:----:|:------:|------------------|
| **R1** | FAIL C0M3 | FAIL C0M2 | **FAIL C1M6** | 🔴 **인용 주체 반전**(Critical) · 요구 4 미반영 · 참조 방향 모호 · 자기선언 구멍 · 측정 지표 부재 · 전파 없음 · 본체 분기 없음 · §11 미등재 |
| **R2** | FAIL **C1**M1 | PASS C0M0 | FAIL **C1**M2 | 🔴 **🔒 emotion 원문이 PUBLIC repo에 복사**(Critical, 2인) · 전파 완료 시제 · 본체 분기 지표 오염 |
| **R3** | PASS | PASS | FAIL C0M2 | 단축이 **조건절 소실** → 무조건 단언 + 3자 불일치 · §12가 R2를 "3인"으로 오기술 |
| **R4**(축소·rtong 승인) | — | — | PASS C0M0 | claude 자진 신고: **최종 문안 저자가 자신이라 품질 판정은 self-approval** + 본체 분기 P·S 미지정 |
| **R5**(초경량·rtong 승인) | **PASS C0M0** | **PASS C0M0** | — | 작성 비참여 2인이 최종 문안 확인 → **절차 갭 해소** |

### 판정 원장

| 주장 | 판정 | 근거 |
|------|:----:|------|
| 템플릿 3곳에 동일 결함 행 존재 | ✅ 확정 | setup:533 · design-system:535 · oraebom:529 — **발견 시점 기준**(이후 oraebom이 자체 ⚠️ 주석을 달아 현재는 바이트 불일치) |
| 접근성 조항 = "제품이 발명하지 않는다" | ❌ **무효 — 요청측 요약이 주체를 반전** | 1차 원문 R-6의 제약 주체는 **DS**. 로컬 read-only 대조로 확정 → **소유자 중립(양방향)으로 확장 채택**(확장은 우리 도메인 판단이지 원문 주장 아님) |
| 이 repo는 private | ❌ **무효** | `gh repo view` → `dakman-io/dakman-setup` **visibility: PUBLIC**. 🔒 emotion 원문 복사가 실제 유출이 될 뻔함 |
| 교정 후 🔒 emotion 원문 잔존 | ✅ **0건** | 마커 6종 python 전수 — emotion 원본 299KB 대조군 non-zero, md·html·project-init 전건 0 |
| md↔html 대칭 | ✅ 확정 | 정규화 후 핵심 11구 **비대칭 0**, 무변경 앵커 대조군 유효 |
| 3자 정합(원칙 8 ↔ §12 ↔ project-init) | ✅ 확정 | `있으면` 잔존 0 |

### 잔여 이견 (지우지 않고 보존 — §9.7)
- **grok**: 원칙 8 본문에 "DS 미사용=`n/a`" 한 토막을 넣으면 원칙 한 줄만 읽어도 미사용 경로가 보인다. **미채택** — §11 표 + 포인터로 성립하고, R5 PASS 이후 새 미검증 편집을 만들지 않기 위해.
- **codex·grok·claude**: 원칙 8이 469자로 13원칙 최장(중앙값 4.9배). **3인 모두 "길이보다 정확성 우선"에 동의** — 분기를 다시 빼면 R3 M-1(조건절 소실) 재발.
- **claude**: `본체` 분기가 DS 본체 repo의 데모/문서 화면 소유를 완전히 규정하진 않는다(퇴화 케이스, 실무 영향 낮음).

---

## 4. 방법론에서 건진 것

1. **held-out은 인용을 원문까지 따라가야 값이 나온다.** R1 Critical(주체 반전)은 리뷰어가 emotion 1차 원문을 직접 열었기 때문에 잡혔다. 요약 경유는 요약자의 오류를 그대로 상속한다.
2. **"공개 범위"는 규율이 아니라 사실로 확인해야 한다.** `gh repo view` 한 번이 "기밀 한 구절쯤"을 "PUBLIC 유출"로 재분류했다. 규율 문장(🔒 로컬-only)만으로는 위반을 못 막았다 — **선언과 위반이 같은 셀에 있었다.**
3. **과공학 교정이 규범을 죽일 수 있다.** R3에서 "원칙 8이 최장"이라는 Minor를 받아 단축했더니 조건절이 소실돼 규범이 거짓 단언이 됐다. **길이는 대리 지표고 정확성이 본지표다.**
4. **리뷰어의 제안을 채택하면 그 리뷰어는 그 문안의 held-out이 아니게 된다.** R4에서 claude가 이걸 자진 신고했다. 채택 = 저자성 이전이고, 종결은 작성 비참여 노드가 해야 한다.
5. **합의 headcount를 반올림하지 마라.** §12가 R2를 "3인"이라 적었지만 실제는 2인(grok은 PASS·같은 지점을 Minor로 분류)이었다. 거짓 합의는 잔여 이견을 지우는 데서 생긴다.
6. **정본은 처방과 함께 결함도 복제한다**(함정 클래스 11-가). 템플릿은 복제력이 가장 큰 정본이라, 여기서의 검토 공백은 곧 복제되는 공백이다.

---

## 5. 미완 (머지 후)

**전파** — 템플릿 수정만으로 배포된 사본은 치유되지 않는다. `cross-project-notify`로 통지 예정(사본 수정은 각 세션 소유):
- **dakman-design-system**(:535 원문 잔존) — 인용 반전 사실도 함께 통지(그쪽 `lessons.md`·요약 경로 점검용)
- **dakman-oraebom**(:529 stale TODO — 이 수정이 도착했음을 알 경로가 없다)

**부수 통지** — claude-config: `grok 0.2.112`가 **trust 프롬프트를 새로 추가**했다(스킬의 0.2.93 실측 노트 "trust 프롬프트 없음"과 어긋남 — 첫 디스패치가 조용히 종료된 원인).
